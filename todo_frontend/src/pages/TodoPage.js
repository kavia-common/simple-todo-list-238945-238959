import React from "react";
import TodoApp from "../todo/TodoApp";

// PUBLIC_INTERFACE
export default function TodoPage() {
  /** Page route that renders the todo application UI. */
  return <TodoApp />;
}
